const parseCode = (str) => {
  // your code here
	let obj = {
		firstName: "Toshi",
		lastName: "mi",
		id: "1"
	}
	 if(parseCode){
		 return obj;
	 }
	 
};

// Do not change the code below
const str = prompt("Enter str: ");
alert(JSON.stringify(parseCode(str)));
